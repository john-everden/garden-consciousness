name: Spell Check
on: [pull_request]

jobs:
  spell-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Check Spelling
        uses: codespell-project/actions-codespell@master
        with:
          ignore_words_list: "Runestone,qualia"
